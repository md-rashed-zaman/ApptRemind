{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "title": "billing.subscription.canceled.v1",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "business_id": { "type": "string", "format": "uuid" },
    "tier": { "type": "string", "enum": ["free"] },
    "max_monthly_appointments": { "type": "integer" },
    "canceled_at": { "type": "string", "format": "date-time" }
  },
  "required": ["business_id", "tier", "max_monthly_appointments", "canceled_at"]
}
