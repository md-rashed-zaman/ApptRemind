# Postgres (local only)
POSTGRES_SUPERUSER=postgres
POSTGRES_SUPERUSER_PASSWORD=postgres

# Per-service DB users (local only)
AUTH_DB_PASSWORD=auth_password
BUSINESS_DB_PASSWORD=business_password
BOOKING_DB_PASSWORD=booking_password
BILLING_DB_PASSWORD=billing_password
SCHEDULER_DB_PASSWORD=scheduler_password
NOTIFICATION_DB_PASSWORD=notification_password
ANALYTICS_DB_PASSWORD=analytics_password

# Kafka
KAFKA_BROKERS=kafka:9092

# Redis (optional; used for gateway distributed rate limiting)
# REDIS_ADDR=redis:6379
# REDIS_PASSWORD=
# REDIS_DB=0

# Stripe (optional; test mode is free)
# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_WEBHOOK_SECRET=whsec_...
# STRIPE_WEBHOOK_TOLERANCE_SECONDS=300
# STRIPE_PRICE_STARTER=price_...
# STRIPE_PRICE_PRO=price_...
# CHECKOUT_SUCCESS_URL=http://localhost:8080/billing/success?session_id={CHECKOUT_SESSION_ID}
# CHECKOUT_CANCEL_URL=http://localhost:8080/billing/cancel?session_id={CHECKOUT_SESSION_ID}

# Billing - Stripe reconciliation (optional; production hardening)
# BILLING_STRIPE_RECONCILE_ENABLED=false
# BILLING_STRIPE_RECONCILE_INTERVAL_SECONDS=300
# BILLING_STRIPE_RECONCILE_BATCH_SIZE=50
# BILLING_STRIPE_RECONCILE_LOCK_KEY=4242001
